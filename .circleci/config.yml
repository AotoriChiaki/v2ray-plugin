version: '2.1'
orbs:
  go: circleci/go@1.12.0
jobs:
  build:
    executor:
      name: go/default
      tag: '1.22.8'
    steps:
      - checkout
      - run: sudo apt-get update && sudo apt-get install -y upx
      - go/load-cache
      - run: go mod tidy && ./build-release.sh
      - go/save-cache
      - store_artifacts:
          path: bin
          destination: bin
workflows:
  main:
    jobs:
      - build
